@model App472.WebUI.Models.AdminUserViewModel
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="card">
    <div class="card-body">
        <br />
        Showing all Orders for all Users
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Username</th>
                    <th scope="col">Email</th>
                    <th scope="col">Orders</th>
                </tr>
            </thead>
            <tbody>
                @foreach (App472.Domain.Entities.Guest guest in Model.Guests)
                {
                    <tr>
                        <td class="">@guest.Id</td>
                        <td scope="row" class="text-left">@guest.FirstName @guest.LastName</td>
                        <td scope="row" class="text-left">@guest.Email</td>
                        <td><a href="@Url.Action("Index", "AdminUserOrder", new{UserId = guest.Id})">View Orders</a></td>
                    </tr>
                }
                @foreach (App472.WebUI.Models.AppUser user in Model.Users)
                {
                    <tr>
                        <td class="">@user.Id</td>
                        <td scope="row" class="text-left">@user.UserName</td>
                        <td scope="row" class="text-left">@user.Email</td>
                        <td><a href="@Url.Action("Index", "AdminUserOrder", new{UserId = user.Id})">View Orders</a></td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>
